import React, { Component } from 'react';

class Main extends Component{
    render(){
        return(
            <h2 className="black-text" style={{'textAlign': 'center'}} >BlockAI</h2>
        );
    }
}

export default Main;